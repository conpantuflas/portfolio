import styled from "styled-components";

const PrincipalContentSkills = styled.div`
  margin: 0 auto;
  position: relative;
  z-index: 0;
  width: 100%;
  overflow-x: hidden;
  overflow-y: visible;
`;

export default PrincipalContentSkills;
